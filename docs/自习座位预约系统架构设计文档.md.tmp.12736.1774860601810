# 自习座位预约系统架构设计文档

## 1. 引言

### 1.1 项目背景
本项目基于高等学校日常学习生活需求，通过信息技术手段实现自习室座位预约系统，提升自习室座位使用率。系统分为学生端和管理端，支持座位预约、签到、提醒、搜索等功能，并集成智能化助手实现自然语言交互。

### 1.2 目的
本架构设计文档旨在描述自习座位预约系统的整体架构、技术选型、组件设计、数据模型及部署方案，为开发团队提供指导，确保系统满足功能需求、非功能需求，并支持DevOps实践。

## 2. 系统概述

### 2.1 功能概述
- **学生端**：查看可用座位、预约座位、签到、接收提醒、搜索座位、历史记录查询、智能助手交互。
- **管理端**：RBAC权限管理、自习室/座位管理、预约记录查看、违约记录管理、系统参数调整。
- **智能化**：通过聊天框实现自然语言查询，如查找空座、靠窗座位等。

### 2.2 用户角色
- **学生**：使用学生端进行预约和签到。
- **管理员**：使用管理端进行系统管理，不同角色有不同权限。

## 3. 架构设计

### 3.1 整体架构
系统采用前后端分离的单体架构，后端作为统一的应用服务，通过模块化设计实现不同业务功能的隔离。整体架构如下（文本表示）：

```
┌─────────────────────────────────────────────────────────────────┐
│                          前端层                                   │
│  ┌──────────────────┐         ┌──────────────────────────┐       │
│  │ 微信小程序/Web    │         │   Web应用 - 管理端        │       │
│  │ 应用 - 学生端     │         └──────────────────────────┘       │
│  └──────────────────┘                                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     后端层（单体应用）                            │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │  预约模块  │  签到模块  │  通知模块  │  智能助手模块  │      │
│  │  管理模块  │ RBAC权限模块                                  │    │
│  └──────────────────────────────────────────────────────────┘    │
│                              │                                    │
│                              ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │                      数据层                               │    │
│  │              MySQL 数据库    │    Redis 缓存              │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
          │                                           ▲
          │                                           │
          ▼                                           │
┌──────────────────────┐     ┌──────────────────────────────┐
│    外部服务           │     │                               │
│                      │     │                               │
│  - 邮件/微信推送服务  │◀────│ 通知模块                      │
│  - 大语言模型API      │◀────│ 智能助手模块                   │
└──────────────────────┘     └──────────────────────────────┘
```

**数据流说明**：
- 前端层通过HTTP/REST API访问后端单体应用。
- 后端各业务模块与数据层交互，完成数据持久化与缓存。
- 通知模块直接调用外部推送服务发送邮件/微信提醒。
- 智能助手模块直接调用大语言模型API处理自然语言交互。
- 外部服务由后端业务模块调用，数据层仅负责数据存储。

### 3.2 架构风格
- **单体架构**：后端作为统一的应用服务，通过模块化设计（Blueprint/插件机制）实现业务功能的隔离与解耦，便于开发、部署和维护。
- **前后端分离**：前端负责用户交互，后端提供RESTful API，两者独立开发与部署。

### 3.3 组件描述
后端单体应用通过模块化设计实现以下功能模块：

- **预约模块**：处理座位预约、取消、搜索逻辑。
- **签到模块**：处理签到验证、违约检测与自动取消。
- **通知模块**：发送预约提醒、违约通知（邮件/微信）。
- **智能助手模块**：集成大语言模型，实现自然语言交互查询。
- **管理模块**：处理座位/自习室管理、系统参数调整。
- **RBAC权限模块**：基于角色的访问控制，权限校验与角色管理。
- **数据库**：MySQL存储用户、座位、预约等核心数据；Redis缓存热点数据，提升查询性能。

## 4. 技术栈

### 4.1 前端
- **学生端**：Vue.js Web应用。
- **管理端**：Vue.js Web应用。
- **UI框架**：Element UI。

### 4.2 后端
- **框架**：Python Flask。
- **API设计**：RESTful API。

### 4.3 数据层
- **数据库**：MySQL。
- **ORM**：SQLAlchemy。

### 4.4 外部集成
- **推送服务**：邮件服务、微信服务号API。
- **大语言模型**：OpenAI API或其他开源模型。

### 4.5 开发工具
- **版本控制**：Git，托管在GitHub。
- **CI/CD**：GitHub Actions流水线，包括自动化测试、构建、部署。

## 5. 数据架构

### 5.1 主要实体
- **用户**：学生、管理员信息。
- **自习室**：自习室信息，包括开放时间、所属院系。
- **座位**：座位信息，包括编号、位置、特殊标记（靠窗、插座）。
- **预约**：预约记录，包括时间、状态（预约中、已签到、违约）。
- **角色**：RBAC角色定义。
- **权限**：角色权限映射。

### 5.2 实体关系图
主要实体关系如下（文本描述）：

- **USER** (用户) -- 1:N --> **RESERVATION** (预约) : 用户进行预约
- **STUDY_ROOM** (自习室) -- 1:N --> **SEAT** (座位) : 自习室包含座位
- **SEAT** (座位) -- 1:N --> **RESERVATION** (预约) : 座位被预约
- **ROLE** (角色) -- 1:N --> **USER** (用户) : 角色分配给用户
- **ROLE** (角色) -- 1:N --> **PERMISSION** (权限) : 角色拥有权限

## 6. 部署架构

### 6.1 单体部署
后端单体应用统一打包、部署，无需服务间通信与协调。

### 6.2 DevOps流水线
- **代码托管**：GitHub代码库。
- **构建**：自动化编译、单元测试。
- **部署**：自动化部署到测试/生产环境。
- **流水线**：集成测试、部署流水线。

### 6.3 环境
- **开发环境**：本地开发。
- **测试环境**：GitHub Actions测试环境。
- **生产环境**：云服务器生产环境（单实例或负载均衡）。

## 7. 安全设计

### 7.1 RBAC
- 角色：超级管理员、管理员、普通管理员等。
- 权限：查看预约、修改座位、调整参数等。
- 实现：后端服务中集成权限检查。

### 7.2 其他安全措施
- 数据加密：敏感数据加密存储。
- API安全：HTTPS、API密钥。
- 日志审计：记录操作日志。

## 8. 非功能需求

### 8.1 性能
- 响应时间：<2秒。
- 并发用户：支持1000+并发。

### 8.2 可扩展性
- 模块化设计，支持后续功能扩展（如增加通宵自习室管理、座位预约排队等）。
- 单体应用支持水平扩展（多实例负载均衡），应对业务增长。

### 8.3 可维护性
- 模块化设计，代码规范。

### 8.4 可用性
- 99.9% uptime，通过监控和备份。

## 9. 风险与缓解

- **技术风险**：选择成熟技术栈（Flask/Vue.js），团队培训，降低学习成本。
- **进度风险**：使用Scrum方法，GitHub Projects管理，模块化开发提升效率。
- **扩展性风险**：单体架构通过模块化设计预留扩展空间，后期可根据业务需求进行服务拆分。

## 10. 结论
本架构设计采用前后端分离的单体架构，为自习座位预约系统提供了清晰、简洁的技术路径。该架构既能保持代码的清晰结构（模块化设计），又不会引入微服务带来的不必要复杂度，非常适合课程项目的开发节奏和团队规模。同时通过DevOps实践，确保项目全流程规范化管理。